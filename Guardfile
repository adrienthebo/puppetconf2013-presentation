# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'rake', :task => 'build' do
  watch(%r{^presentation.text$})
  watch(%r{^templates/**/*})
  watch(%r{^presentation/*})
end
